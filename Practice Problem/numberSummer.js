// write a program that continuously prompts the user for numbers until they enter 0
// after they never 0, the program should print out the total of all numbers they input

const prompt = require("prompt-sync")({ sigint: true })